{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // `page.evaluate` wykonuje się w przeglądarce, więc jego ciała sięgają po
    // `window` i `document` — mimo że sam test biegnie w Node.
    "lib": ["ES2022", "DOM"],
    "types": ["node"]
  },
  "include": ["tests", "playwright.config.ts"]
}
